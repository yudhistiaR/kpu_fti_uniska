const LoginLayout = ({ children }) => {
  return (
    <main className="max-w-md h-screen m-auto flex justify-center items-center px-4">
      {children}
    </main>
  );
};

export default LoginLayout;
